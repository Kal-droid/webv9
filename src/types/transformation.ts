export interface TransformationStep {
  id: number;
  title: string;
  description: string;
  image: string;
}

export interface JoinUsSection {
  title: string;
  description: string;
  image: string;
}